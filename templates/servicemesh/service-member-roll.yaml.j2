#jinja2: lstrip_blocks: "True"
apiVersion: maistra.io/v1
kind: ServiceMeshMemberRoll
metadata:
  name: default
  namespace: istio-system
spec: 
 {% if servicemesh_members.create %}
   {% for p in range(servicemesh_members.start,servicemesh_members.end) %}
    - {{ p }}
   {% endfor %}
 {% endif %}
